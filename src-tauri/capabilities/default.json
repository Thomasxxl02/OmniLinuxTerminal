{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default capabilities for OmniLinux Terminal (durci : permissions minimales, pas de plugin shell)",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
