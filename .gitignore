node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Rust / Tauri build artifacts (src-tauri)
src-tauri/target/
src-tauri/gen/
# NOTE: src-tauri/Cargo.lock is intentionally COMMITTED (reproducible builds for the Rust backend)

# Typegen specta (généré au build debug, non importé par le frontend)
src/types/
